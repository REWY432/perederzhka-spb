# Perederzhka SPB 🐕

Система управления передержкой собак с современным glassmorphism дизайном.

## 🌟 Возможности

- ✅ Управление собаками и владельцами
- ✅ Календарь броней с визуализацией
- ✅ Учёт издержек (памперсы, химчистка и т.д.)
- ✅ Праздничные дни с надбавками
- ✅ Финансовые отчёты и статистика
- ✅ LTV по каждой собаке
- ✅ Напоминания о заездах/выездах
- ✅ PWA поддержка (работает как приложение)
- ✅ Адаптивный дизайн для всех устройств
- ✅ Современный glassmorphism UI

## 🚀 Быстрый старт

### 1. Настройка Supabase

1. Создайте аккаунт на [supabase.com](https://supabase.com)
2. Создайте новый проект
3. Перейдите в SQL Editor
4. Выполните код из файла `supabase-schema.sql`
5. Скопируйте URL проекта и анонимный ключ (Settings → API)

### 2. Настройка приложения

1. Клонируйте репозиторий:
```bash
git clone <your-repo-url>
cd perederzhka-spb
```

2. Установите зависимости:
```bash
npm install
```

3. Откройте файл `src/App.jsx` и замените учётные данные Supabase:
```javascript
const SUPABASE_URL = 'YOUR_SUPABASE_URL';
const SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY';
```

### 3. Запуск в режиме разработки

```bash
npm run dev
```

Приложение откроется на `http://localhost:5173`

### 4. Деплой на GitHub Pages

1. Отредактируйте `vite.config.js` и установите корректный `base`:
```javascript
base: '/repository-name/'  // Замените на имя вашего репозитория
```

2. Соберите проект:
```bash
npm run build
```

3. Деплой на GitHub Pages:

**Вариант А: Используя gh-pages**
```bash
npm install -D gh-pages
```

Добавьте в `package.json`:
```json
"scripts": {
  "deploy": "npm run build && gh-pages -d dist"
}
```

Запустите:
```bash
npm run deploy
```

**Вариант Б: Вручную**
1. Перейдите в Settings → Pages
2. Выберите Source: "GitHub Actions"
3. Создайте файл `.github/workflows/deploy.yml`:

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: ['main']
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci
      - run: npm run build
      - uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/deploy-pages@v4
        id: deployment
```

4. Закоммитьте и запушьте изменения

## 📱 PWA (Progressive Web App)

После деплоя вы можете:
- Установить приложение на телефон/компьютер
- Работать офлайн (с кэшированием)
- Получать push-уведомления (если настроить)

На iOS (Safari):
1. Откройте сайт
2. Нажмите "Поделиться"
3. "Добавить на домашний экран"

На Android (Chrome):
1. Откройте сайт
2. Нажмите меню (три точки)
3. "Установить приложение"

## 🎨 Структура базы данных

### Таблица `dogs`
- `id` - UUID, первичный ключ
- `name` - Кличка собаки
- `breed` - Порода (опционально)
- `breed_size` - Размер: small/medium/large
- `comment` - Комментарий
- `owner_name` - Имя владельца (опционально)
- `owner_phone` - Телефон владельца (опционально)

### Таблица `bookings`
- `id` - UUID, первичный ключ
- `dog_id` - Ссылка на собаку
- `check_in` - Дата заезда
- `check_out` - Дата выезда
- `status` - Статус: active/upcoming/completed/cancelled
- `base_price_per_day` - Базовая цена за день
- `custom_price_per_day` - Своя цена (опционально)
- `holiday_days` - Количество праздничных дней
- `holiday_price_add` - Доплата за праздничный день
- `notes` - Заметки

### Таблица `expenses`
- `id` - UUID, первичный ключ
- `booking_id` - Ссылка на бронь
- `name` - Название издержки
- `amount` - Сумма

## 💰 Расчёт стоимости

Формула:
```
Обычные дни = (Всего дней - Праздничных дней) × Цена за день
Праздничные дни = Праздничных дней × (Цена за день + Праздничная надбавка)
Итого = Обычные дни + Праздничные дни - Издержки
```

## 🎯 Базовые тарифы

- Малые породы: 1500 ₽/день
- Средние породы: 2000 ₽/день
- Большие породы: 3000 ₽/день

Можно указать свою цену при создании брони.

## 🔔 Напоминания

Система показывает уведомления за день до:
- Заезда собаки (статус "Будущая")
- Выезда собаки (статус "Активная")

## 📊 Отчёты

### Доступная статистика:
- Доход за период (завершённые брони)
- Потенциальный доход (активные и будущие брони)
- Издержки за период
- Количество броней
- Топ собак по выручке
- LTV (Lifetime Value) каждой собаки

## 🛠️ Технологии

- **Frontend**: React 18
- **Styling**: CSS с Glassmorphism
- **Database**: Supabase (PostgreSQL)
- **Build**: Vite
- **PWA**: vite-plugin-pwa
- **Hosting**: GitHub Pages
- **Date**: date-fns
- **Icons**: lucide-react

## 📝 Использование

### Добавление собаки
1. Перейдите во вкладку "Собаки"
2. Нажмите "Добавить собаку"
3. Заполните данные
4. Сохраните

### Создание брони
1. Перейдите в "Календарь" или "Брони"
2. Нажмите "+" или "Новая бронь"
3. Выберите собаку
4. Укажите даты
5. Настройте цену и праздничные дни
6. Сохраните

### Добавление издержек
1. Откройте бронь
2. Нажмите значок "$"
3. Укажите название и сумму
4. Сохраните

### Просмотр отчётов
1. Перейдите во вкладку "Отчёты"
2. Выберите период
3. Анализируйте статистику

## 🔒 Безопасность

⚠️ **Важно**: В текущей версии нет авторизации. Любой, кто знает URL, может получить доступ к данным.

Для продакшн-использования рекомендуется:
1. Включить Row Level Security в Supabase
2. Добавить аутентификацию
3. Настроить CORS и домены

## 🐛 Известные ограничения

- Нет синхронизации в реальном времени (нужно обновлять страницу)
- Базовые уведомления (только в приложении)
- Нет экспорта данных в Excel/PDF

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте консоль браузера (F12)
2. Убедитесь, что Supabase настроен правильно
3. Проверьте, что все таблицы созданы
4. Проверьте политики доступа (RLS)

## 📄 Лицензия

MIT

---

Сделано с ❤️ для передержки собак в Санкт-Петербурге
